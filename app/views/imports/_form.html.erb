<%= form_with(model: import, local: true) do |form| %>
  <% if import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(import.errors.count, "error") %> prohibited this import from being saved:</h2>

      <ul>
        <% import.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Select #{type.pluralize} file" %>
    <%= form.file_field :file %>
  </div>

  <div class="field" style="display:none;">
    <%= form.text_field :type, value: type %>
  </div>

  <div class="actions">
    <%= form.submit "Import #{type.pluralize}" %>
  </div>
<% end %>
